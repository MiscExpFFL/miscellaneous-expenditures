const D=window.LEAGUE_DATA,Y=window.SEASON_2026,Q=new URLSearchParams(location.search);const e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const team=m=>Y.teams.find(t=>t.manager===m), arch=m=>D.archetypes.find(a=>a.manager===m), rec=m=>D.records.find(r=>r.manager===m);
function nav(){return `<header class="site-head"><nav class="nav shell"><a class="brand" href="index.html">ME<span>FFL</span></a><div class="navlinks"><a href="weeks.html">Weekly Write-Ups</a><a href="trophy-case.html">Trophies</a><a href="franchises.html">Franchises</a><a href="head-to-head.html">H2H</a><a href="draft-history.html">Draft</a><a href="transactions.html">Moves</a><a href="waiver-wire.html">Waivers</a><a href="keepers.html">Keepers</a><a href="power-rankings.html">Power</a><a href="playoff-odds.html">Odds</a><a href="record-book.html">Records</a><a href="hall-of-shame.html">Shame</a><a href="press-room.html">Press</a></div><details class="mobile-menu"><summary aria-label="Open site menu"><span class="menu-icon">☰</span><span>Menu</span></summary><div class="mobile-menu-links"><a href="weeks.html">Weekly Write-Ups</a><a href="trophy-case.html">Trophies</a><a href="franchises.html">Franchises</a><a href="head-to-head.html">H2H</a><a href="draft-history.html">Draft</a><a href="transactions.html">Moves</a><a href="waiver-wire.html">Waivers</a><a href="keepers.html">Keepers</a><a href="power-rankings.html">Power</a><a href="playoff-odds.html">Odds</a><a href="record-book.html">Records</a><a href="hall-of-shame.html">Shame</a><a href="press-room.html">Press</a></div></details></nav></header>`}
function foot(){return `<footer><div class="shell footer-inner"><div><strong>Miscellaneous Expenditures</strong><p>Bad decisions, good friends, real money and fake grudges.</p></div><div>Updated: ${e(Y.lastUpdated)}</div></div></footer>`}
function hero(t,c=''){return `<section class="page-hero"><div class="shell"><p class="eyebrow">MISCELLANEOUS EXPENDITURES</p><h1>${e(t)}</h1><p class="lede">${e(c)}</p></div></section>`}
function sec(t,h,i='',ey=''){return `<section class="section"><div class="shell"><div class="section-head"><div>${ey?`<p class="eyebrow dark">${e(ey)}</p>`:''}<h2>${t}</h2></div>${i?`<p class="section-intro">${e(i)}</p>`:''}</div>${h}</div></section>`}
function card(t,b,ey=''){return `<div class="card">${ey?`<p class="eyebrow dark">${e(ey)}</p>`:''}<h3>${t}</h3><p>${b}</p></div>`}
function ranks(){return Y.teams.map(t=>`<a class="rank-card" href="franchise.html?manager=${encodeURIComponent(t.manager)}"><div class="rank-num">${t.rank}</div><div class="rank-team"><strong>${e(t.team)}</strong><small>${e(t.manager)} · ${e(t.projection)}</small></div><div class="rank-grade"><strong>${e(t.grade)}</strong><small>Yahoo ${e(t.yahoo)}</small></div></a>`).join('')}
function publishedWeeks(){return Object.entries(Y.weekly||{}).map(([w,d])=>({week:Number(w),...d})).filter(d=>d.writeup&&!/write-up pending/i.test(d.headline||'')&&!/bracket TBD/i.test(d.headline||'')).sort((a,b)=>b.week-a.week)}
function homepageWeeklies(){let p=publishedWeeks();if(!p.length)return '';let latest=p[0],older=p.slice(1,4);let old=older.length?`<div class="home-weekly-recent"><p class="eyebrow dark">RECENT WEEKS</p><div class="home-weekly-recent-grid">${older.map(d=>`<a class="home-weekly-mini" href="week.html?week=${d.week}"><small>WEEK ${d.week}</small><strong>${e(d.headline)}</strong><span>Read the write-up →</span></a>`).join('')}</div></div>`:'';return `<section class="section home-weekly-section"><div class="shell"><div class="section-head"><div><p class="eyebrow dark">FROM THE LEAGUE DESK</p><h2>Latest Weekly Write-Up</h2></div><p class="section-intro">The newest recap lives here on the front page and stays archived on its own Week page.</p></div><article class="home-weekly-feature"><div class="home-weekly-copy"><div class="home-weekly-meta"><span>WEEK ${latest.week}</span><span>${e((latest.status||'write-up').toUpperCase())}</span></div><h3>${e(latest.headline)}</h3><p>${e(latest.writeup)}</p><div class="home-weekly-actions"><a class="button primary" href="week.html?week=${latest.week}">Open the full Week ${latest.week} page</a><a class="home-archive-link" href="weeks.html">All weekly write-ups →</a></div></div><aside class="home-weekly-side"><p class="eyebrow">GAME OF THE WEEK</p><h4>${e(latest.gameOfWeek||'To be earned')}</h4><p>${e(latest.gotwCopy||'')}</p><div class="home-weekly-side-rule"></div><p class="eyebrow">TOILET WATCH</p><p>${e(latest.toiletWatch||'Pending.')}</p></aside></article>${old}</div></section>`}
function home(){let h=D.history.map(x=>card(`${x.year} — ${e(x.champion)}`,`<b>Runner-up:</b> ${e(x.runnerUp)}<br><b>Toilet:</b> ${e(x.toilet)}<br><br>${e(x.headline)}`)).join('');return `<section class="hero"><div class="shell"><p class="eyebrow">2026 SEASON HUB</p><h1>Miscellaneous<br><em style="color:var(--lime);font-weight:400">Expenditures</em></h1><p class="lede">Three years of bad beats, keeper theft, playoff heartbreak and one back-to-back champion nobody wants to hear from anymore.</p><div class="hero-actions"><a class="button primary" href="draft-history.html#2026-draft">See Who Won the Draft</a><a class="button ghost" href="weeks.html">Enter the Weekly Archive</a></div></div></section>`+sec('The story so far',`<div class="grid3">${h}</div>`,'Records are nice. The real history is who won, who collapsed and who had to sit behind a microphone.','THREE YEARS OF POOR DECISIONS')+homepageWeeklies()+`<section class="section dark"><div class="shell"><div class="section-head"><div><p class="eyebrow">CURRENT POWER</p><h2>Who we think is good</h2></div></div>${ranks()}</div></section>`}
function franchises(){let h=Y.teams.map(t=>{let a=arch(t.manager),r=rec(t.manager);return `<a class="card" href="franchise.html?manager=${encodeURIComponent(t.manager)}"><p class="eyebrow dark">${e(t.manager)}</p><h3>${e(t.team)}</h3><div class="stat">${e(r?.record||'')}</div><p><b>${e(a?.title||'')}</b><br>${e(a?.body||'')}</p></a>`}).join('');return hero('Franchise Files','Every current franchise, from rings to Toilets.')+sec('Know your enemy',`<div class="grid2">${h}</div>`,'','THE RECEIPTS')}
function franchise(){let m=Q.get('manager')||'Tommy',t=team(m),a=arch(m),r=rec(m);if(!t)return hero('Franchise not found');let k=(D.keepers||[]).filter(x=>x.manager===m);return hero(`${m} — ${t.team}`,a?.title||'')+`<section class="section"><div class="shell profile-top"><div class="profile-box"><div class="big-record">${e(r?.record||'')}</div><p>${e(a?.body||'')}</p><div class="mini-stats"><div class="mini-stat"><b>${e(r?.pf||'')}</b><small>Career PF</small></div><div class="mini-stat"><b>${e(t.projection)}</b><small>2026 projection</small></div><div class="mini-stat"><b>${e(t.grade)}</b><small>Our draft grade</small></div></div></div><div class="callout"><h3>${e(a?.title||'')}</h3><p>${e(t.outlook)}</p></div></div></section>`+sec('2026 outlook',`<div class="grid2">${card('The good',e(t.best))}${card('How this goes wrong',e(t.concern))}</div>`)}
function transactions(){let rows=Y.transactionLeaderboard.map(r=>`<tr><td>${e(r[0])}</td><td>${r[1]||'—'}</td><td>${r[2]||'—'}</td><td>${r[3]||'—'}</td><td><b>${r[4]}</b></td></tr>`).join('');return hero('Transaction Leaderboard','Some managers build through the draft. Owen rebuilds the plane while it is in the air.')+sec('The leaderboard',`<div class="table-wrap"><table><thead><tr><th>Manager</th><th>2023</th><th>2024</th><th>2025</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table></div><div style="margin-top:18px"><a class="button primary" href="waiver-wire.html">Open the 2026 Waiver Wire →</a></div>`)}
function waiverBody(w){
  const faab=(w.faab||[]).sort((a,b)=>a.priority-b.priority).map(x=>`<tr><td><b>${e(x.priority)}</b></td><td>${e(x.manager)}</td><td>${e(x.team)}</td><td>$${e(x.spent)}</td><td><b>$${e(x.remaining)}</b></td><td>${e(x.claimsWon)}</td></tr>`).join('');
  const avail=(w.topAvailable||[]).length?(w.topAvailable||[]).map((p,i)=>`<tr><td><b>${i+1}</b></td><td>${e(p.name)}</td><td>${e(p.position||'')}</td><td>${e(p.nflTeam||'')}</td><td>${e(p.status||'FA')}</td><td>${e(p.recent||'—')}</td><td>${p.faabSuggestion?e(p.faabSuggestion):'—'}</td></tr>`).join(''):`<tr><td colspan="7"><div class="empty-state"><b>Waiting for Yahoo data.</b><br>Once the API bridge is connected, the best available QB/RB/WR/TE options will populate here automatically.</div></td></tr>`;
  const tx=(w.recentTransactions||[]).length?(w.recentTransactions||[]).map(x=>`<div class="activity-card"><div><span class="activity-type">${e(x.type||'MOVE')}</span><strong>${e(x.manager||x.team||'')}</strong></div><p>${e(x.description||'')}</p><small>${e(x.time||'')}</small></div>`).join(''):`<div class="empty-state"><b>No 2026 transactions yet.</b><br>Completed adds, drops, waiver claims and trades will land here after Yahoo is connected.</div>`;
  const notes=(w.notes||[]).map(n=>`<li>${e(n)}</li>`).join('');
  return `<div class="live-status"><span class="status-dot ${w.apiStatus&&w.apiStatus.toLowerCase().includes('connected')?'on':'off'}"></span><div><b>${e(w.apiStatus||'Manual mode')}</b><small>Last update: ${e(w.lastUpdated||Y.lastUpdated)}</small></div></div>`+
    `<div class="waiver-grid">${card('Live data connection',`The page is already built. The Yahoo API will feed it through a secure backend; no Yahoo secret is ever stored in GitHub.<br><br><b>Auto-refresh target:</b> every ${e((window.MEFFL_API&&window.MEFFL_API.refreshSeconds)||120)} seconds.`,'API READY')}${card('Privacy rule',`We can publish completed waiver activity and FAAB spent. We will <b>not</b> expose pending bids before waivers process.`,'NO CHEATING')}</div>`+
    `<div class="waiver-section"><div class="section-head"><div><p class="eyebrow dark">MONEY LEFT TO BURN</p><h2>FAAB & Waiver Priority</h2></div><p class="section-intro">Everybody begins 2026 with $100 FAAB.</p></div><div class="table-wrap"><table><thead><tr><th>Priority</th><th>Manager</th><th>Team</th><th>Spent</th><th>Remaining</th><th>Claims Won</th></tr></thead><tbody>${faab}</tbody></table></div></div>`+
    `<div class="waiver-section"><div class="section-head"><div><p class="eyebrow dark">WHO IS STILL OUT THERE?</p><h2>Top Available Players</h2></div><p class="section-intro">Yahoo availability + MEFFL waiver analysis once connected.</p></div><div class="table-wrap"><table><thead><tr><th>#</th><th>Player</th><th>Pos</th><th>NFL</th><th>Status</th><th>Recent</th><th>Suggested FAAB</th></tr></thead><tbody>${avail}</tbody></table></div></div>`+
    `<div class="waiver-section"><div class="section-head"><div><p class="eyebrow dark">THE TAPE</p><h2>League Activity</h2></div><p class="section-intro">Completed moves only.</p></div><div class="activity-feed">${tx}</div></div>`+
    `<div class="callout"><h3>How this will work live</h3><ul>${notes}</ul><p class="yahoo-attribution">Fantasy data provided by Yahoo Fantasy</p></div>`;
}
function waivers(){let w=Y.waiverWire||{};return hero('Waiver Wire','The league’s live free-agent board, FAAB ledger and transaction tape.')+`<section class="section"><div class="shell"><div id="waiver-live-content">${waiverBody(w)}</div></div></section>`}
async function refreshWaivers(){
  const cfg=window.MEFFL_API||{}, url=cfg.waiverEndpoint;
  if(!url) return;
  try{
    const r=await fetch(url,{cache:'no-store'}); if(!r.ok) throw new Error('HTTP '+r.status);
    const data=await r.json();
    data.apiStatus=data.apiStatus||'Connected to Yahoo'; data.lastUpdated=data.lastUpdated||new Date().toLocaleString();
    const el=document.getElementById('waiver-live-content'); if(el) el.innerHTML=waiverBody(data);
  }catch(err){
    const el=document.querySelector('.live-status b'); if(el) el.textContent='Yahoo connection temporarily unavailable';
  }
}
function power(){return hero('Weekly Power Rankings','The page where confidence goes to die one Sunday at a time.')+sec(`Week ${Y.week} snapshot`,ranks(),'','THE CURRENT PECKING ORDER')}
function bars(f,red=false){return [...Y.teams].sort((a,b)=>b[f]-a[f]).map(t=>`<div class="odds-row"><strong>${e(t.manager)}</strong><div class="bar${red?' red':''}"><span style="width:${t[f]}%"></span></div><b>${t[f]}%</b></div>`).join('')}
function odds(){return hero('Playoff Odds','Six teams chase the money. One eventually meets the microphones.')+sec('Make the bracket',bars('playoffOdds'))+sec('Win it all',bars('titleOdds'))+sec('Press-conference risk',bars('pressRisk',true))}
function records(){let rows=Y.recordBook.map(r=>`<tr><td>${e(r[0])}</td><td>${e(r[1])}</td><td><b>${e(r[2])}</b></td><td>${e(r[3])}</td></tr>`).join('');return hero('Record Book','The numbers nobody is allowed to forget.')+sec('The league ledger',`<div class="table-wrap"><table><thead><tr><th>Record</th><th>Holder</th><th>Number</th><th>Receipt</th></tr></thead><tbody>${rows}</tbody></table></div>`)}
function weeks(){let h='';for(let w=1;w<=17;w++){let d=Y.weekly[String(w)];h+=`<a class="week-card${w===Y.week?' current':''}" href="week.html?week=${w}"><div class="week-status">${e(d.status)}</div><h3>Week ${w}</h3><p>${e(d.headline)}</p></a>`}return hero('2026 Weekly Write-Ups','')+sec('Every week gets receipts',`<div class="week-grid">${h}</div>`)}
function week(){let w=Number(Q.get('week')||1),d=Y.weekly[String(w)];let ms=(d.matchups||[]).map(m=>`<div class="matchup"><span>${e(m[0])}${m[2]?`<small class="proj">Yahoo ${e(m[2])}</small>`:''}</span><span>VS</span><span>${e(m[1])}${m[3]?`<small class="proj">Yahoo ${e(m[3])}</small>`:''}</span></div>`).join('')||'<p>Bracket TBD.</p>';let aw=(d.awards||[]).map(a=>card(e(a[1]),'',e(a[0]))).join('')||card('Awards pending','Whatever stupidity the week earns.');return hero(`Week ${w} — 2026`,d.headline)+`<section class="section"><div class="shell grid2"><div><p class="eyebrow dark">THE WRITE-UP</p><h2>${e(d.headline)}</h2><p style="font-size:18px">${e(d.writeup)}</p></div><div class="gotw"><p class="eyebrow">GAME OF THE WEEK</p><h3>${e(d.gameOfWeek)}</h3><p>${e(d.gotwCopy)}</p></div></div></section>`+sec('The slate',ms)+`<section class="section alt"><div class="shell grid3">${card('Standings movement',e(d.standingsMovement),'THE TABLE')}${card('Who smells trouble?',e(d.toiletWatch),'TOILET WATCH')}${card('Quote of the week',e(d.quote),'FROM THE GROUP CHAT')}</div></section>`+sec('Weekly awards',`<div class="grid3">${aw}</div>`)+`<section class="section dark"><div class="shell"><h2>Power rankings</h2>${ranks()}</div></section>`}
const routes={home,franchises,franchise,transactions,waivers,power,odds,records,weeks,week};
const currentPage=document.body.dataset.page;
document.body.innerHTML=nav()+(routes[currentPage]||home)()+foot();
if(currentPage==='waivers'){refreshWaivers();const cfg=window.MEFFL_API||{};if(cfg.waiverEndpoint)setInterval(refreshWaivers,Math.max(60,cfg.refreshSeconds||120)*1000);}